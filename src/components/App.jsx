import { GlobalStyle } from "styles/GlobalStyle";
import { Theme } from "styles/Theme";

export const App = () => {
  return (
    <div>
      <Theme>
        <GlobalStyle />
        тут щось буде...
        </Theme>
    </div>
  );
};
