export const selectShoppingList = state => state.shoppingList.products;
export const selectIsLoading = (state) => state.shoppingList.isLoading;
export const selectShoppingListUpateIsLoading = (state) =>
  state.shoppingList.shoppingListUpateIsLoading;
